import React from 'react';

const FormMovie = (props) => {
  return (
    <div>
      <h1>Form Movie</h1>
      {/* <Form.Item label="Rating">
        <Rate
          value={edit ? data.platform : ''}
          onChange={handleChange('platform')}
        />
      </Form.Item> */}
    </div>
  );
};

export default FormMovie;
